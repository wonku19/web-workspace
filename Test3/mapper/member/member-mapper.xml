<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis">

    <insert id="insertMember" parameterType="org.kh.member.model.vo.MemberVO">
        INSERT INTO member (user_no, user_id, user_pw, user_Name, user_addr, reg_addr)
        VALUES (#{userNo}, #{userId}, #{userPw}, #{userName}, #{userAddr}, #{regAddr})
    </insert>

</mapper>